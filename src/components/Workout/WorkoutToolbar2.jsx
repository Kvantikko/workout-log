




const WorkoutToolbar2 = ({ handleDrawerOpen }) => {
/*     console.log("Rendering WorkoutToolbar2222222222222222222");
    const workoutStarted = useSelector(state => state.workout.workoutStarted)
    const exercises = useSelector(state => state.exercises)
    const stopWatchIsActive = useSelector(state => state.stopWatch.isActive)
    const [showModal, setShowModal] = useState(false)
    const isSmallScreen = useMediaQuery('(min-width:900px)');
    const isExpanded = useSelector(state => state.drawer)

    const [openCancelModal, setOpenCancelModal] = useState(false);
    const [openFinishModal, setOpenFinishModal] = useState(false);


    const dispatch = useDispatch()

    const handleModalOpen = () => {
        setShowModal(true)
    }

    const handleFinishClick = () => {
        if (exercises.length === 0) {
            toast.warning('Add at least one exercise before finishing!')
            return
        }
        handleModalOpen()
    }

    const handleClear = () => {
        dispatch(clearWorkout())
        dispatch(clearExercises())
        dispatch(clearSets())
        dispatch(stopWatch())
        dispatch(resetWorkout())
  
    }

    const handleOpenFinishModal = () => {
        if (exercises.length === 0) {
            toast.warning('Add at least one exercise before finishing!')
            return
        }
        setOpenFinishModal(true)
    } */

    return (
       

            <div>dadawdwa</div>


    )
}

export default WorkoutToolbar2